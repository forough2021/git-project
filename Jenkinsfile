pipeline {
agent any
stages {
   stage('build') {
     steps {
       echo "hello world"
       }
    }
    
   stage('test') {
    steps {
     echo "hello test"
    }
   }
   stage('deploy') {
     steps {
       echo "hello deploy"
     }
   }
}
}
